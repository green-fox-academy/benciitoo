public class a13SecondsInADay {
    public static void main(String[] args) {
        int currentHours = 14;
        int currentMinutes = 34;
        int currentSeconds = 42;

        int hours = 23;
        int mins = 59;
        int secs = 60;

        System.out.println((hours - currentHours) + " hours " + (mins - currentMinutes) + " minutes " + (secs - currentSeconds) + " seconds are left from the day");


        // Write a program that prints the remaining seconds (as an integer) from a
        // day if the current time is represented by the variables
    }
}